config BR2_PACKAGE_KMOD_RTL8723BT
	bool "Realtek RTL8723b USB Bluetooth"
	depends on BR2_LINUX_KERNEL
	help
	  realtek rtl8723b usb bluetooth card driver

comment "kmod_rtl8723bt needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
