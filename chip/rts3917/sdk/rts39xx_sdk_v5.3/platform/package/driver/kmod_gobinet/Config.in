config BR2_PACKAGE_KMOD_GOBINET
	bool "Gobinet driver for Quectel EC2x modem"
	depends on BR2_LINUX_KERNEL
	help
	  Gobinet driver for Quectel EC20 USB LTE Modem

comment "kmod_gobinet needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
