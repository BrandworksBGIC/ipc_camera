config BR2_PACKAGE_KMOD_RTL8822CU
	bool "Realtek RTL8822CU USB WiFi"
	depends on BR2_LINUX_KERNEL
	help
	  realtek rtl8822cu usb wifi card driver

comment "kmod_rtl8822cu needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
