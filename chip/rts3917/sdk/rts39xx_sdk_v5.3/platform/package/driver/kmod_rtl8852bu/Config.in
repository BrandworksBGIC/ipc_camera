config BR2_PACKAGE_KMOD_RTL8852BU
	bool "Realtek RTL8852B USB WiFi"
	depends on BR2_LINUX_KERNEL
	help
	  realtek rtl8852bu usb wifi card driver

comment "kmod_rtl8852bu needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
